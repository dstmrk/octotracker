# ========================================
# OctoTracker - Configurazione Ambiente
# ========================================

# === OBBLIGATORIO ===
# Token del bot Telegram (ottieni da @BotFather)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# === MODALITÀ BOT ===
# Modalità: "polling" o "webhook"
# - polling: Il bot chiede a Telegram se ci sono messaggi (default, più semplice)
# - webhook: Telegram invia messaggi al tuo server (richiede tunnel/IP pubblico, più veloce)
BOT_MODE=polling

# === CONFIGURAZIONE WEBHOOK (solo se BOT_MODE=webhook) ===
# URL pubblico dove Telegram invierà i messaggi
# Esempio: https://octotracker.tuodominio.xyz
WEBHOOK_URL=

# Porta locale per il webhook server (default: 8443)
WEBHOOK_PORT=8443

# Token segreto per validare richieste (opzionale ma consigliato)
# Genera con: openssl rand -hex 32
WEBHOOK_SECRET=

# === SCHEDULER ===
# Ora dello scraping tariffe (0-23, default: 9)
SCRAPER_HOUR=9

# Ora del controllo e notifiche (0-23, default: 10)
CHECKER_HOUR=10

# === KEEP-ALIVE (solo modalità polling) ===
# Intervallo keep-alive in minuti (0 = disabilitato, default: 0)
# Nota: Non necessario in modalità webhook o su Docker locale
KEEPALIVE_INTERVAL_MINUTES=0
